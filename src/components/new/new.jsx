import React from 'react/addons';
import ReactMarkdown from 'react-markdown';

const New = module.exports = React.createClass({
  render() {
    return (
      <div className="new">
        New
      </div>
    );
  }
});